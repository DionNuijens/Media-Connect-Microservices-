spring.application.name=Auth-Service

server.port=8080

spring.rabbitmq.host=${RABBITMQ_HOST:localhost}
spring.rabbitmq.port=${RABBITMQ_PORT:5672}
spring.rabbitmq.username=${RABBITMQ_USER:guest}
spring.rabbitmq.password=${RABBITMQ_PASSWORD:guest}
spring.rabbitmq.connection-timeout=10000

spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/mc_auth}
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASSWORD:GragasLover1!}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

jwt.private-key=classpath:jwt/app.key
jwt.public-key=classpath:jwt/app.pub
jwt.ttl=30m

#management.endpoints.web.exposure.include=health,readiness,liveness
#management.endpoint.health.probes.enabled=true

management.endpoints.web.exposure.include=prometheus,health,info,readiness,liveness
management.endpoint.prometheus.enabled=true
#management.metrics.export.prometheus.enabled=true
#spring.application.name=my-service